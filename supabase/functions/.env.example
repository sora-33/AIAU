# Edge Functions のローカル環境変数。
# コピーして supabase/functions/.env を作成する（.env はコミットされない）。
# 起動: supabase functions serve --env-file supabase/functions/.env

# 抽出器: mock（API キー不要・規則ベース）| openai
LLM_PROVIDER=mock

# LLM_PROVIDER=openai のとき必須
OPENAI_API_KEY=
# 任意（既定: gpt-4o-mini）
LLM_MODEL=
