# TabiTogether — プロダクト仕様（ドラフト）

## 1. 概要

**TabiTogether** は、複数人の旅行計画で散らばりがちな希望、移動情報、予約リンク、意思決定を一つの画面に集める AI 共同旅行プランナーです。

参加者それぞれの希望と旅行条件を入力すると、AI が無理のない順序の旅行案を作成します。参加者は提案にコメントまたは投票し、合意した内容を共有プランとして確認・編集できます。

## 2. 解決する課題

複数人で旅行を計画すると、次の情報や判断がチャット、各自の端末、複数の予約サイトに分散します。

- 行きたい場所、やりたいこと、避けたいこと
- 日付、予算、出発地、移動上の制約
- チケット・予約ページのリンクと移動時刻
- 日程案への確認、変更、合意の経緯

その結果、会話を遡る作業や重複確認が増え、計画役の一人に調整負荷が集中します。

## 3. 対象ユーザー

友人、家族、コミュニティなどで、旅行または週末のお出かけを複数人で計画する人。

## 4. 提供価値

1. 参加者ごとの希望を一か所に集約する。
2. AI が希望と旅行条件を要約し、行き先・時間順・提案理由を含む旅行案に変換する。
3. コメントまたは投票により、全員が同じ画面で案を確認し合意形成できる。
4. 決定したプランにチケット URL、移動時刻、メモを紐づけ、最新情報を共有できる。

## 5. MVP

ハッカソンでは、次の一本道を動作する形で完成させます。

1. 旅行を一つ作成する。
2. 参加者ごとの希望を入力・保存する。
3. AI が希望を要約し、1日分の旅行案を生成する。
4. 参加者が案にコメントまたは投票する。
5. 選ばれた案にチケット URL・移動時刻・メモを登録し、共有画面で表示する。

### MVP 画面

| 画面         | 目的           | 主な情報・操作                |
| ---------- | ------------ | ---------------------- |
| 旅行ダッシュボード  | 旅行の全体状況を把握する | 目的地、日付、参加者、選択中のプラン     |
| 希望入力       | 参加者の条件を集める   | 方向性、予算、やりたいこと、制約       |
| AI提案       | 旅行案を比較・検討する  | 時間順の旅程、提案理由、コメントまたは投票  |
| 共同編集・共有プラン | 合意内容を更新・共有する | チケット URL、移動時刻、メモ、確定プラン |

### MVP 機能要件

`Must` は最終デモまでに実装する機能ですが、完成させる順番とデモへの影響に応じて3つのレベルに分けます。上位レベルが未完成の場合、次のレベルへ進まずに完成させます。

| レベル | 到達状態 | デモで示せる価値 |
| --- | --- | --- |
| Level 1 | ログインし、共有された旅行の条件と希望を保存できる | 計画に必要な情報を一か所へ集約・共有できる |
| Level 2 | AI が希望を基に編集可能な旅行案を生成できる | AI が条件を具体的な旅程へ変換できる |
| Level 3 | 参加者の反応を基に案を確定し、実務情報とともに共有できる | 複数人の合意から共有プランまで完結できる |

`Should` は Level 3 完了後、時間に余裕がある場合に実装します。

| ID    | 優先度            | 機能要件                | 受入条件                                                                                |
| ----- | -------------- | ------------------- | ----------------------------------------------------------------------------------- |
| FR-01 | Must / Level 1 | ログインできる           | 利用者がログイン状態を開始・終了できる。ログイン後、利用者を識別した状態で旅行を作成または開ける。認証方式は実装速度を優先して選定する。                |
| FR-02 | Must / Level 1 | 旅行を URL で共有できる    | 旅行ごとに共有用 URL を取得できる。別の利用者がその URL を開くと、同じ旅行のダッシュボードを表示できる。                           |
| FR-03 | Must / Level 1 | 旅行を作成・編集できる         | タイトル、日付、出発地、予算を入力して旅行を1件保存できる。保存後、ダッシュボードに旅行の概要が表示される。                              |
| FR-04 | Must / Level 1 | 参加者を登録できる           | 少なくとも2名分の表示名を旅行に追加できる。ログイン利用者と参加者の対応付けは、デモに必要な範囲で扱う。                               |
| FR-05 | Must / Level 1 | 参加者ごとの希望を入力・保存できる   | 各参加者について、方向性、やりたいこと、避けたいこと、制約を入力・更新できる。保存した内容は画面を再表示しても確認できる。                       |
| FR-06 | Must / Level 1 | 旅行データを保存・再表示できる     | 旅行、参加者、希望、提案、反応、共有プランの情報を永続化し、ページ再読み込み後も同じ内容を確認できる。                                 |
| FR-07 | Must / Level 2 | AI が旅行案を生成できる       | 旅行条件と参加者の希望を渡して、1日分の時間順プランを1案以上生成・表示できる。生成中・失敗時を画面上で判別でき、失敗時は再試行または用意したサンプル案を表示できる。 |
| FR-08 | Must / Level 2 | AI 提案の内容を確認・編集できる   | 各提案に、時間帯、場所または活動、移動に関する情報、提案理由を表示する。利用者は表示内容を編集できる。                                 |
| FR-09 | Must / Level 3 | 提案に対して合意の意思を残せる     | 参加者が提案にコメントまたは投票の少なくとも一方を登録できる。誰による反応かと内容または票数を表示できる。                               |
| FR-10 | Must / Level 3 | 選んだ提案を共有プランとして確定できる | 提案を1つ選択して共有プランに設定できる。ダッシュボードと共有画面で、現在選ばれているプランを識別できる。                               |
| FR-11 | Must / Level 3 | 共有プランに実務情報を登録できる    | 各予定またはプラン全体に、チケット・予約 URL、移動時刻、メモを追加・編集できる。入力済み情報を共有画面に表示できる。                        |
| FR-12 | Should         | 変更を参加者の画面へ反映できる     | 希望・反応・共有プランの変更が、他の参加者の画面に再読み込みなしで反映される。Supabase Realtime の利用候補とする。                  |

### 実装上の補足

- データ保存には Supabase Database を利用候補とする。旅行、参加者、希望、提案、反応、共有プランを必要最小限のデータ構造で扱う。
- AI 呼び出しには、LLM API キーをクライアントに公開しない API 層（例: Supabase Edge Functions）を利用候補とする。
- Auth は FR-01 を満たすために実装する。認証方式や招待フローの作り込みは最小限にし、RLS は共有範囲を制御する必要が生じた場合の追加候補とする。
- 外部の時刻表・施設情報が取得できない場合も、手入力の時刻・URL・メモで同じデモシナリオを完結させる。

### MVP 完了の判定

次のシナリオを通しで実演できれば MVP 完了とする。

1. 利用者がログインし、旅行を作成して共有 URL を取得する。
2. 別の利用者が共有 URL を開き、2名分の希望を保存する。
3. AI が希望を踏まえた1日分の旅行案を生成する。
4. 参加者が提案へコメントまたは投票し、1案を共有プランとして確定する。
5. 確定プランに移動時刻・チケット URL・メモを登録し、再読み込み後も共有画面で確認する。

## 6. 入力・出力

### 旅行条件と参加者の希望

- 旅行の方向性: アート、アウトドア、インドア、グルメ、ゆっくり過ごす等
- 日付、出発地、予算
- 行きたい場所、やりたいこと
- 避けたいこと、行けない時間、移動の制約

### AI 提案の期待値

- 参加者の希望と制約を短く要約する。
- 午前・午後・夕方など、時間順の 1 日プランを提案する。
- 移動に無理がない順序にする。
- 各案の提案理由を簡潔に示す。
- 提案は誤る可能性がある前提で、参加者が確認・編集できる形にする。

例: 「東京発・日帰り・1人8,000円程度。アートとカフェが好きだが、長距離歩行は避けたい」という条件から、午前・午後・夕方のプランを 2 案作成する。

## 7. デモの成功条件

2人以上の参加者が異なる希望を入力し、AI 提案を表示する。その後、別の参加者がコメントまたは投票を行い、共有できる確定プランが一画面で確認できること。

## 8. 今回扱わないこと

- 実予約・決済
- 完全自動のチケット取得
- すべての旅行形態への対応

外部の時刻表・施設情報は誤りや接続不安定の可能性があるため、MVP では URL・時刻・メモを手入力でも登録できることを優先します。

## 9. MVP 完了後の候補

- ログインと招待リンク
- リアルタイムでの編集反映
- 複数案の比較
- 地図表示
- 予約情報・チケット画像の添付
- 実際の鉄道ダイヤ・施設営業時間の外部 API 連携

## 10. 実装に関する前提

データ保存・認証・共同編集には Supabase、フロントエンドには Next.js / React、AI 提案には LLM API を候補とします。ただし、採用技術・データモデル・画面詳細はチーム決定後に確定します。

認証情報・API キーはリポジトリにコミットせず、環境変数と `.env.local` で管理します。
